<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.youren.bbs.dao.UserDao">

	<insert id="create" parameterType="User" useGeneratedKeys="true" keyProperty="id">
		insert into t_user(username,password,sex,age,tel,email,headshot,create_time,state,type)
		values(#{username},#{password},#{sex},#{age},#{tel},#{email},#{headshot},#{createTime},#{state},#{type})
	</insert>

	<update id="update" parameterType="User">
		update t_user set password=#{password},sex=#{sex}
	</update>

	<delete id="delete" parameterType="long">
		delete from t_user where id=#{id}
	</delete>

	<select id="findById" parameterType="long" resultType="User">
		select id,username,password,tel,sex,age,email,headshot,state,type,create_time createTime from t_user where id=#{id}
	</select>

	<select id="findByUsername" parameterType="string" resultType="User">
		select id,username,password,sex,age,email,headshot,state,type,create_time createTime from t_user where username=#{username}
	</select>

	<select id="findAll" resultType="User">
		select id,username,password,tel,sex,age,tel,email,headshot,state,type,create_time createTime from t_user
	</select>
</mapper>